import 'package:flutter/material.dart';

class AppColors {
  // Cores Principais baseadas nas diretrizes do sistema
  static const Color primaryDark = Color(0xFF0F4C3A); // Verde Escuro
  static const Color primaryLight = Color(0xFF27AE60); // Verde Claro
  static const Color primaryHover = Color(0xFF219653); // Hover Verde Claro
  static const Color background = Color(0xFFF8F9FA); // Fundo cinza bem claro
  static const Color white = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFFFFFFF); // Fundo de cards
  
  static const Color textMain = Color(0xFF2C3E50); // Texto principal
  static const Color textMuted = Color(0xFF7F8C8D); // Textos secundários
  static const Color borderColor = Color(0xFFEAECEE); // Bordas
}
